name: Merging CI

on:
  merge_group:
  pull_request:

jobs:
  append-committer-signed-off-by:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - run: |
          scripts/append-committer-signed-off-by.sh
